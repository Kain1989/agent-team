# Team-status portal — Phase 1 (read-only) backend.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
# Test-only (kept here so `pip install -r requirements.txt` sets up a dev box):
httpx>=0.27          # FastAPI TestClient dependency
pytest>=8.0

[project]
name = "textkit"
version = "0.1.0"
description = "Tiny text-utilities library — the demo project the agent team works on."
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
